import './credits.scss'

import Vue from 'vue'

export default Vue.extend({
    template: require('./credits.html'),

    data() {
        return {
        }
    },

    ready() {
    },

    methods: {
    }
})
